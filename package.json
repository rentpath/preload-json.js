{
  "version": "0.0.1",
  "name": "parallel-data",
  "description": "Load data quickly with JSONP",
  "license": "MIT",
  "homepage": "https://github.com/rentpath/parallel-data",
  "repository": {
    "type": "git",
    "url": "https://github.com/rentpath/parallel-data.git"
  },
  "bugs": {
    "url": "https://github.com/rentpath/parallel-data/issues"
  },
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf lib",
    "compile": "babel -d lib src",
    "compile:watch": "npm run compile -- --watch",
    "lint": "eslint src test",
    "lint:fix": "npm run lint -- --fix",
    "test": "mocha --compilers js:babel-core/register --timeout 5000 --recursive",
    "test:watch": "npm run test -- --watch",
    "test:cov": "babel-node `npm bin`/istanbul cover `npm bin`/_mocha -- --recursive",
    "preversion": "npm run lint && npm test",
    "version": "npm run compile && git add -A lib",
    "postversion": "git push --follow-tags",
    "demo": "serve demo"
  },
  "devDependencies": {
    "babel-cli": "^6.4.0",
    "babel-core": "^6.4.0",
    "babel-plugin-add-module-exports": "^0.1.2",
    "babel-preset-es2015": "^6.3.13",
    "babel-preset-stage-0": "^6.3.13",
    "chai": "^3.4.1",
    "eslint": "^2.1.0",
    "eslint-config-rentpath": "^1.1.0",
    "istanbul": "^1.0.0-alpha",
    "mocha": "^2.3.4",
    "jsdom": "8.0.2",
    "jsdom-global": "1.6.1",
    "sinon": "^1.17.3"
  },
  "engines": {
    "node": ">=4.2.1",
    "npm": ">=3"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
